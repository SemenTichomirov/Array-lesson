# Создание списков
empty_array = []
num_array = [1, 2, 3, 4, 5]
Mixed_array = [1, "текст", 3.14, True, [1, 2]]

# Разные способы создания
array1 = list("hello")  # ['h', 'e', 'l', 'l', 'o']
array2 = [i for i in range(5)]  # [0, 1, 2, 3, 4]
array3 = [0] * 5  # [0, 0, 0, 0, 0]

# array = [10, 20, 30, 40, 50] - Как получить такой список 2-3 способа

# Положительные индексы (слева направо)
array = [i * 25 for i in range(5)]
first = array[0]  # 10
second = array[1]  # 20

# Отрицательные индексы (справа налево)
last = array[-1]  # 50
prevlast = array[-2]  # 40

array = [0, 1, 2, 3, 4, 5]
          Срезы
# array[start:stop:step]
first3 = array[0:3]  # [0, 1, 2]
fhird = array[2:]  # [2, 3, 4, 5]
to_fhird = array[:3]  # [0, 1, 2]
every_second = array[::2]  # [0, 2, 4]
back = array[::-1]  # [5, 4, 3, 2, 1, 0]

# [25, 75, 125], [125, 75, 25] - Как получить такие массивы


array = [1, 2, 3, 4, 5]

# Замена элемента
array[0] = 100  # [100, 2, 3, 4, 5]

# Замена среза
array[1:3] = [200, 300]  # [100, 200, 300, 4, 5]

array = [1, 2, 3]

# append() - добавление в конец
array.append(4)  # [1, 2, 3, 4]

# insert() - вставка по индексу
array.insert(1, 1.5)  # [1, 1.5, 2, 3, 4]

# extend() - расширение списка
array.extend([5, 6])  # [1, 1.5, 2, 3, 4, 5, 6]


# Начальный массив array = [1, 2, 3, 4, 5, 6]
# Конечный массив ['Привет', 'Друг', 5, 6, 'конец', 7, 8, 'массива']



array = [1, 2, 3, 4, 5, 3]

# remove() - удаление по значению (первое вхождение)
array.remove(3)  # [1, 2, 4, 5, 3]

# pop() - удаление по индексу (по умолчанию последний)
last = array.pop()  # удаляет 3, возвращает его
element = array.pop(1)  # удаляет элемент с индексом 1

# del - удаление по индексу или срезу
del array[0]  # удаляет первый элемент
del array[1:3]  # удаляет срез

# clear() - очистка списка
array.clear()  # []

array = [1, 2, 3, 2, 4, 2]

# index() - поиск индекса элемента
indx = array.index(3)  # 2
indx_pos = array.index(2, 3)  # ищем 2 начиная с 3 позиции

# count() - подсчет вхождений
quant = array.count(2)  # 3

# in - проверка наличия
if 3 in array:
    print("Элемент найден")

    array = [3, 1, 4, 1, 5, 9, 2]

# sort() - сортировка списка на месте
array.sort()  # [1, 1, 2, 3, 4, 5, 9]
array.sort(reverse=True)  # [9, 5, 4, 3, 2, 1, 1]

# sorted() - создание нового отсортированного списка
new_array = sorted(array)

array = [1, 2, 3]

# copy() - создание копии
copy_array = array.copy()

# reverse() - разворот списка
array.reverse()  # [3, 2, 1]

# len() - длина списка (встроенная функция)
len_array = len(array)  # 3